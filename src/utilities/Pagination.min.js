import{useState,useEffect}from"react";const calculateRange=(data,rowsPerPage)=>{const range=[],num=Math.ceil(data.length/rowsPerPage);let i=1;for(let i=1;i<=num;i++)range.push(i);return range},sliceData=(data,page,rowsPerPage)=>data.slice((page-1)*rowsPerPage,page*rowsPerPage),useTable=(data,page,rowsPerPage)=>{const[tableRange,setTableRange]=useState([]),[slice,setSlice]=useState([]);return useEffect(()=>{const range=calculateRange(data,rowsPerPage);setTableRange([...range]);const slice=sliceData(data,page,rowsPerPage);setSlice([...slice])},[data,setTableRange,page,setSlice]),{slice:slice,range:tableRange}};export default useTable;